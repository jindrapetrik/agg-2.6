package agg;

/**
 * Inner join styles for stroke rendering
 */
public enum InnerJoinE {
    BEVEL,  // Bevel inner join
    MITER,  // Miter inner join
    JAG,    // Jag inner join
    ROUND;  // Round inner join
}
